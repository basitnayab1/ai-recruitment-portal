"use client";

/**
 * @deprecated Use `FilterSelect` / `FilterDateInput` from
 * `@/components/hr/search/filter-fields`. These no longer auto-submit on change.
 */
export {
  FilterSelect as AutoSubmitSelect,
  FilterDateInput as AutoSubmitDateInput,
} from "@/components/hr/search/filter-fields";
