"use client";

/**
 * @deprecated Use `FilterSearchInput` from `@/components/hr/search/filter-fields`.
 * Kept as a thin alias so any leftover imports keep compiling.
 * Does NOT auto-submit on keystroke.
 */
export { FilterSearchInput as DebouncedSearchInput } from "@/components/hr/search/filter-fields";
